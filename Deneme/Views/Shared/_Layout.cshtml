<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Projeler</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />

    <link href="~/css/site.css" rel="stylesheet" />
</head>
<body class="d-flex flex-column min-vh-100">
    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">Yazılım Projelerim</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link active" href="/Project/Index">Ana Sayfa</a></li>
                    <li class="nav-item"><a class="nav-link" href="/Project/About">Hakkımda</a></li>
                    <li class="nav-item"><a class="nav-link" href="/Contact/Message">İletişim</a></li>
                    <li class="nav-item"><a class="nav-link" href="/Admin/Dashboard">Admin Girişi</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- MAIN CONTENT -->
    <main class="container mt-4 flex-grow-1">
        <div class="row">
            <!-- MAIN BODY -->
            <div class="col-md-8">
                @RenderBody()
            </div>

            <!-- SIDEBAR -->
            @* <div class="col-md-4"> *@
                <!-- Search Box -->
                @* <div class="card mb-4">
                    <div class="card-header">Blog Ara</div>
                    <div class="card-body">
                        <form method="post" action="#">
                            <div class="input-group">
                                <input type="text" name="query" class="form-control" placeholder="Ara..." />
                                <button class="btn btn-primary" type="submit"><i class="bi bi-search"></i></button>
                            </div>
                        </form>
                    </div>
                </div> *@

        @*         <!-- Tags -->
              <div class="card mb-4">
                    <div class="card-header">Etiketler</div>
                    <div class="card-body">
                        <span class="badge bg-secondary me-1">tech</span>
                        <span class="badge bg-secondary me-1">software</span>
                        <span class="badge bg-secondary me-1">engineer</span>
                        <span class="badge bg-secondary me-1">teknoloji</span>
                    </div>
                </div>
            </div>
        </div> *@
    </main>

    <!-- FOOTER -->
    <footer class="bg-dark text-light text-center py-3 mt-auto">
        <div class="container">
            <p class="mb-0">&copy; 2025 Deneme Blog - Tüm Hakları Saklıdır.</p>
            <small class="text-muted">Design by <a class="text-decoration-none text-light" href="http://www.coolwebtemplates.net/">Cool Website Templates</a></small>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>